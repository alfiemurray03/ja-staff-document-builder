// This page has been removed — the template wizard is no longer used.
// The Builders Hub (/builders) replaces the old template library.
import { Navigate } from 'react-router-dom';
export default function TemplateWizardPage() {
  return <Navigate to="/builders" replace />;
}
